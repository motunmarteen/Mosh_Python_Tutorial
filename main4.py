# Create a Python script that extracts the first and last characters of a user's inputted name.

name = input("What is your name? ")
firstLetter = name[0]
lastLetter = name[-1]

print(f"{firstLetter}{lastLetter}")